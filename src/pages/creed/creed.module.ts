import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CreedPage } from './creed';

@NgModule({
  declarations: [
    CreedPage,
  ],
  imports: [
    IonicPageModule.forChild(CreedPage),
  ],
})
export class CreedPageModule {}
